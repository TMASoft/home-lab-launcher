# Screenshots

Release screenshots and GIFs live here and are referenced from the project README. Use the neutral demo data from `npm run dev:seed`; do not capture maintainer-specific hostnames, private service URLs, tokens, local paths, or personal plugin configuration.

Current beta captures:

- `logo.svg` — project logo used at the top of the README.
- `launchpad.png` — anonymous or signed-in launchpad with services and optional plugin sections.
- `appearance-customization.png` — Appearance tab with branding/theme controls.
- `admin-overview.png` — Admin Overview beta readiness checklist and warnings.
- `plugin-trust.png` — plugin manager trusted-code acknowledgement.
- `user-management.png` — user management page.
- `command-palette.png` — role-aware command palette (built-in Vaporwave theme preset).
- `service-discovery.png` — Admin → Discovery Compose import with reviewed candidates (Vaporwave preset).
- `first-admin-setup.png` — first-run Create first Admin modal (default first-boot look; saved themes apply only after setup).
